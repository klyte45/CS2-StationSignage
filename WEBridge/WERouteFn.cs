using System;
using Unity.Entities;

namespace StationSignage.BridgeWE
{
    public static class WERouteFn
    {
        public static string GetTransportLineNumber(Entity reference) => throw new NotImplementedException("Stub only!");
    }
}